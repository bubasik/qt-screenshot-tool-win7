name: Build Qt Screenshot Tool (64-bit)

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build-win64:
    runs-on: windows-2022

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Qt 5.12.12 (64-bit, MinGW)
        uses: jurplel/install-qt-action@v4
        with:
          version: '5.12.12'
          arch: 'win64_mingw73'
          cache: true
          cache-key-prefix: qt-win64-mingw73-5.12.12
          set-env: true

      - name: 🔍 Verify toolchain
        shell: pwsh
        run: |
          Write-Host "=== Qt version ==="
          qmake --version
          
          Write-Host "`n=== Compiler architecture ==="
          $arch = g++ -dumpmachine 2>&1
          Write-Host "Architecture: $arch"
          if ($arch -notlike "*x86_64*") {
            Write-Error "❌ Not 64-bit compiler!"
            exit 1
          }
          Write-Host "✅ 64-bit confirmed"

      - name: 🛠️ Build project (Release)
        run: |
          qmake ScreenshotTool.pro "CONFIG+=release"
          mingw32-make -j4
        shell: cmd

      - name: 🔍 Debug locate executable
        run: |
          echo === Searching for .exe files ===
          dir /s /b *.exe
          echo.
          echo === Current directory structure ===
          dir
        shell: cmd

      - name: 📦 Deploy Qt dependencies (auto-detect .exe)
        shell: pwsh
        run: |
          # Ищем .exe в текущей директории и подпапках
          $exePath = Get-ChildItem -Path . -Filter "*.exe" -Recurse | Select-Object -First 1 -ExpandProperty FullName
          
          if (-not $exePath) {
            Write-Error "❌ No .exe file found after build!"
            exit 1
          }
          
          Write-Host "✅ Found executable: $exePath"
          Write-Host "Running windeployqt..."
          
          windeployqt --release "$exePath"
          
          # Копируем .exe в отдельную папку для архивации
          $deployDir = "deploy"
          New-Item -ItemType Directory -Path $deployDir -Force | Out-Null
          Copy-Item -Path "$exePath" -Destination $deployDir -Force
          
          # Копируем все зависимости (dll, plugins)
          $exeDir = Split-Path $exePath -Parent
          Copy-Item -Path "$exeDir\*.dll" -Destination $deployDir -ErrorAction SilentlyContinue
          Copy-Item -Path "$exeDir\platforms" -Destination $deployDir -Recurse -ErrorAction SilentlyContinue
          Copy-Item -Path "$exeDir\imageformats" -Destination $deployDir -Recurse -ErrorAction SilentlyContinue
          
          Write-Host "✅ Deployment completed in $deployDir"

      - name: 💾 Package for Windows
        run: |
          Compress-Archive -Path deploy\* -DestinationPath ScreenshotTool-win64-qt5.12.12.zip -Force
        shell: pwsh

      - name: 🚀 Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ScreenshotTool-win64-qt5.12.12
          path: ScreenshotTool-win64-qt5.12.12.zip
          retention-days: 30

      - name: ✅ Success message
        if: success()
        run: |
          echo.
          echo ========================================
          echo  ✅ BUILD SUCCESSFUL!
          echo  📦 Artifact: ScreenshotTool-win64-qt5.12.12.zip
          echo  💻 Works on Windows 7+ (64-bit)
          echo ========================================
        shell: cmd
