# 📸 Qt Скриншотер с поддержкой Windows 7

![Windows 7 Compatible](https://img.shields.io/badge/Windows%207-SP1%2B-brightgreen?logo=windows)
![Qt 5.12.12](https://img.shields.io/badge/Qt-5.12.12-blue?logo=qt)
![MinGW 7.3](https://img.shields.io/badge/MinGW-7.3-orange?logo=gnu)

Простое, но функциональное приложение для создания скриншотов с выделением области, поддержкой тем оформления и горячими клавишами. **Специально оптимизировано для работы на устаревших системах, включая Windows 7 SP1**.

---

## ✨ Возможности

- 🖼️ **Полноэкранный скриншот** — захват всего экрана одним кликом
- ✏️ **Выделение области мышью** — интерактивное выделение с визуальной обратной связью:
  - Полупрозрачный оверлей поверх экрана
  - Синяя пунктирная рамка выделения
  - Отображение размера области в реальном времени (например: `800 x 600`)
  - Отмена выделения через `Esc` или ПКМ
- 🎨 **4 темы оформления**:
  - Светлая (`#F8F9FA` / `#212529`)
  - Тёмная (`#2D2D30` / `#E0E0E0`)
  - Бирюзовая (`#B2DFDB` / `#004D4D`)
  - Матрица (`#000000` / `#00FF41` — неоново-зелёный на чёрном фоне)
- ⌨️ **Горячие клавиши**:
  - `Ctrl+Shift+S` — скриншот всего экрана
  - `Ctrl+Shift+A` — выделение области
  - `Ctrl+S` — сохранить скриншот
  - `Ctrl+C` — копировать в буфер обмена
- 💾 **Экспорт** — сохранение в PNG/JPEG с автоматической генерацией имени файла
- 📋 **Копирование** — мгновенное копирование в буфер обмена для вставки в другие приложения

---

## 💻 Поддерживаемые операционные системы

| ОС | Статус | Требования |
|----|--------|------------|
| **Windows 7 SP1 (64-bit)** | ✅ Полная поддержка | Обновления: [KB2533623](https://www.microsoft.com/en-us/download/details.aspx?id=26764), [KB2999226](https://www.microsoft.com/en-us/download/details.aspx?id=49093) |
| **Windows 8/8.1 (64-bit)** | ✅ Полная поддержка | — |
| **Windows 10/11 (64-bit)** | ✅ Полная поддержка | — |
| **Windows 7 (32-bit)** | ⚠️ Не поддерживается | Приложение собрано только для 64-битных систем |
| **Linux/macOS** | ❌ Не поддерживается | Проект ориентирован исключительно на Windows |

> ⚠️ **Важно:** Приложение **НЕ РАБОТАЕТ** на 32-битных системах. Для максимальной совместимости с устаревшим ПО рекомендуется использовать 64-битную версию Windows 7 SP1 с установленными критическими обновлениями.

---

## ⚙️ Технические требования

| Компонент | Версия | Примечание |
|-----------|--------|------------|
| **Qt** | 5.12.12 | Последняя версия с официальной поддержкой Windows 7 |
| **Компилятор** | MinGW 7.3 (64-bit) | Входит в поставку Qt 5.12.12 |
| **Архитектура** | x86_64 (64-bit) | Только 64-битная сборка |
| **C++ стандарт** | C++11 | Минимальные требования к компилятору |
| **Размер .exe** | ~15 МБ | После `windeployqt` — ~45 МБ с зависимостями |

---

## 🔧 Сборка из исходного кода

### Вариант 1: Локальная сборка (рекомендуется)

#### Требования
- Windows 10/11 (64-bit) для сборки
- [Qt 5.12.12 Online Installer](https://download.qt.io/archive/qt/5.12/5.12.12/)
- При установке выберите компонент: **`Qt 5.12.12 → MinGW 7.3.0 64-bit`**

#### Шаги
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/ваш-логин/qt-screenshot-tool-win7.git
cd qt-screenshot-tool-win7

# 2. Сгенерируйте Makefile
qmake ScreenshotTool.pro "CONFIG+=release"

# 3. Соберите проект
mingw32-make -j4

# 4. Разверните зависимости (создаст папку release/ с .exe и DLL)
windeployqt --release release\ScreenshotTool.exe

# 5. Запустите приложение
release\ScreenshotTool.exe
```

> 💡 **Совет:** Для упрощения используйте **Qt Creator** — откройте `.pro` файл и нажмите «Собрать».

---

### Вариант 2: Сборка через GitHub Actions (автоматическая)

1. Создайте репозиторий на GitHub и загрузите исходный код
2. Добавьте файл `.github/workflows/build.yml` из этого репозитория
3. Запустите сборку вручную:
   - Зайдите в репозиторий → **Actions** → **Build Qt Screenshot Tool (64-bit)**
   - Нажмите **«Run workflow»** → **«Run»**
4. После завершения сборки скачайте артефакт `ScreenshotTool-win64-qt5.12.12.zip` из раздела **Artifacts**

> ✅ **Преимущество:** Не требует установки Qt на вашу машину — сборка происходит в облаке.

---

## 📦 Структура проекта

```
qt-screenshot-tool-win7/
├── .github/workflows/
│   └── build.yml              # GitHub Actions для автоматической сборки
├── ScreenshotTool.pro         # Проектный файл Qt (важные настройки ниже ↓)
├── main.cpp                   # Точка входа приложения
├── screenshottool.h/.cpp      # Основное окно приложения + горячие клавиши
├── regionselector.h/.cpp      # Модуль выделения области мышью
├── themes.h                   # 4 темы оформления (включая "Матрицу")
└── README.md                  # Этот файл
```

---

## ⚠️ Критически важные настройки в `ScreenshotTool.pro`

```qmake
CONFIG += c++11 release windows  # ← ОБЯЗАТЕЛЬНО: 'windows' обеспечивает точку входа WinMain
DESTDIR = release                # ← Гарантирует, что .exe будет в папке release/
QMAKE_CXXFLAGS += -Wno-deprecated-copy  # ← Подавляет шумные предупреждения от старого Qt

# НЕ ДОБАВЛЯТЬ:
# win32:RC_FILE = app.rc  ← Вызовет ошибку "app.rc not found" если файл отсутствует
```

---

## 🚫 История проблем и их решения (чтобы вы не повторили наших ошибок)

| Проблема | Симптомы | Решение | Почему это произошло |
|----------|----------|---------|----------------------|
| **`app.rc not found`** | `mingw32-make[1]: *** No rule to make target 'app.rc'` | Удалить строку `RC_FILE = app.rc` из `.pro` | Файл `app.rc` отсутствовал в репозитории, но указывался в проекте |
| **`undefined reference to WinMain`** | Ошибка линковки с `WinMain@16` | Добавить `CONFIG += windows` в `.pro` | Без этого флага линковщик ищет `main()`, а не `WinMain()` для GUI-приложений |
| **Смешение архитектур** | `undefined reference to '__imp__ZN12QApplicationC1ERiPPci'` | Использовать ТОЛЬКО 64-битную сборку (`win64_mingw73`) | 32-битный Qt пытался линковаться с 64-битным компилятором из глобального PATH |
| **Дублирование метода** | `redefinition of 'void RegionSelector::keyPressEvent'` | Удалить дублирующуюся реализацию в `regionselector.cpp` | Метод `keyPressEvent` был определён дважды в одном файле |
| **`QShortcut` без `setToolTip`** | `'class QShortcut' has no member named 'setToolTip'` | Удалить вызовы `setToolTip()` для `QShortcut` | Метод `setToolTip()` существует только у `QWidget`, но не у `QShortcut` |
| **Неполный тип `QKeyEvent`** | `invalid use of incomplete type 'class QKeyEvent'` | Добавить `#include <QKeyEvent>` в `screenshottool.cpp` | Заголовок не был подключён, хотя использовался в `keyPressEvent()` |
| **`.exe` не в `release/`** | `release\ScreenshotTool.exe does not exist` | Добавить `DESTDIR = release` в `.pro` | По умолчанию Qt может класть `.exe` в корень проекта |
| **Шумные предупреждения** | Сотни строк `deprecated-copy` | Добавить `-Wno-deprecated-copy` в `QMAKE_CXXFLAGS` | MinGW 14+ строже проверяет устаревший код в заголовках Qt 5.12 |

---

## 🔐 Безопасность и ограничения на Windows 7

| Функция | Статус | Примечание |
|---------|--------|------------|
| **Захват защищённого контента** | ❌ Недоступно | Не захватывает:<br>- Видео в полноэкранном режиме (через защищённые API)<br>- UAC-диалоги безопасности |
| **Поддержка нескольких мониторов** | ✅ Работает | Захватывает только основной монитор (`primaryScreen()`) |
| **HDR-мониторы** | ⚠️ Не поддерживается | Windows 7 не имеет API для работы с HDR |
| **DPI scaling** | ⚠️ Ограниченная поддержка | Работает только на мониторах с 100% масштабированием |

> ⚠️ **Важно для 2026 года:** Windows 7 официально не поддерживается с 2020 года. Использование этой ОС в сетевой среде **крайне небезопасно**. Рекомендуется использовать приложение только в изолированных системах (промышленные АРМ, кассы, банкоматы).

---

## 📥 Скачать готовую сборку

Готовые версии доступны в разделе [Releases](https://github.com/ваш-логин/qt-screenshot-tool-win7/releases):

| Файл | Размер | Описание |
|------|--------|----------|
| `ScreenshotTool-win64-qt5.12.12.zip` | ~45 МБ | Полная версия со всеми зависимостями |
| `ScreenshotTool-win64-portable.zip` | ~15 МБ | Минимальная версия (требует установленного Qt 5.12.12) |

**Инструкция по установке:**
1. Скачайте и распакуйте архив в любую папку
2. Запустите `ScreenshotTool.exe`
3. Готово! Приложение не требует установки и не оставляет следов в реестре

---

## 🤝 Участие в проекте

Приветствуются:
- Исправления багов для Windows 7
- Новые темы оформления
- Улучшение алгоритма выделения области
- Добавление базовых аннотаций (стрелки, текст)

Не принимаются:
- Фичи, требующие современных Windows API (Windows.Graphics.Capture и т.д.)
- Поддержка 32-битных систем (слишком сложно для поддержки)
- Зависимости от .NET Framework или других внешних рантаймов

---

## 📜 Лицензия

Проект распространяется под лицензией **MIT** — используйте свободно в личных и коммерческих целях.

```
MIT License

Copyright (c) 2026 Ваше Имя

Разрешается использовать, копировать, изменять и распространять программное
обеспечение без ограничений, при условии сохранения уведомления об авторских
правах и лицензии.
```

---

## 💬 Поддержка

Если у вас возникли проблемы со сборкой или запуском на Windows 7:
1. Проверьте наличие обновлений **KB2533623** и **KB2999226**
2. Убедитесь, что используется **64-битная версия** Windows 7 SP1
3. Создайте [Issue](https://github.com/ваш-логин/qt-screenshot-tool-win7/issues) с полным логом ошибки

> ✨ **Совет от разработчика:** Если вы всё ещё используете Windows 7 в 2026 году — рассмотрите переход на [Windows 10 LTSC](https://learn.microsoft.com/en-us/windows/ltsc/) или [ReactOS](https://reactos.org/) для лучшей безопасности и поддержки современного ПО.
